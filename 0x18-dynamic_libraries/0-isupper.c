#include "main.h"

/**
 * _isupper - checks for uppercase character
 * @elem: the character to be checked
 * Return: true or false
 */
int _isupper(int elem)
{
	return (elem >= 'A' && elem <= 'Z');
}
